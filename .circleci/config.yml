version: 2
jobs:
  build:
    docker:
      - image: eulertour/manim:circleci
    steps:
      - checkout
      - run:
        command: python3 -m pytest mobject/test.py
        environment:
          MEDIA_DIR: media
