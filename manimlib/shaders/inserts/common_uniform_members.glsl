    // What every mobject has, whatever kind it is. Declared first in each kind's
    // block, so that the inserts reading them work wherever they are used.
    float is_fixed_in_frame;
    vec3 shading;
    vec4 clip_plane0;
    vec4 clip_plane1;
    vec4 clip_plane2;
    vec4 clip_plane3;
